import React from "react";

const DesktopTaskbar = () => {
  return (
    <nav className="absolute w-full h-[5%] bottom-0 left-0 flex items-center justify-center bg-black">
      Taskbar
    </nav>
  );
};

export default DesktopTaskbar;
